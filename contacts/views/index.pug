extends layout

block content
  .container
    .content
      h1= title
      hr
      br
      if error
        .alert.alert-danger #{error}
      else
        table.table.table-striped.table-bordered.table-hover
          if contacts
            thead
              tr
                th IDe
                th Firsto Name
                th Lasto Name
                th EMailo
            tbody
              for contact in contacts
                tr
                  td= contact.id
                  td= contact.firstname
                  td= contact.lastname
                  td= contact.email
          else
            form(action="/seed", method="POST")
                button(type="submit").btn.btn-primary Seeda Data
            br
            br
            tr
              td
                | No contactos foundos
